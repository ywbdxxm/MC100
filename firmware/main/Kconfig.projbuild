menu "MC100 application"

config MC100_APP_PRODUCT
    bool "Use the autonomous product recording loop"
    default y
    help
        Select the product supervisor/runtime entry point. Disable this option
        for the EVT_USB_BENCH diagnostic application; that mode is intended for
        explicit bench builds and does not provide autonomous recording.

endmenu
