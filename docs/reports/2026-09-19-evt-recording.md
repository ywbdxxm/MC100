# MC100 USB 台架录音开发验证

## 本轮范围

用户已提供 MC100 实板，唯一允许使用的端口为 **COM7**。已插 SD 卡、未接电池，仅 USB 供电。优先打通麦克风采集、WAV 文件写入及读取验证，后续再优化自动语音触发、功耗、阈值和长期可靠性。

本轮明确使用 EVT_USB_BENCH 测试入口，不把无电池状态误判为产品电池供电正常。不会自动格式化、删除或覆盖卡上文件；不操作其他串口、不更改 eFuse、不更换 SDK。

## 已取得证据

- COM7 成功连接 ESP32-S3 QFN56 revision v0.2，原生 USB Serial/JTAG。
- Flash 读取识别为 8 MB，Quad，芯片配置电压 3.3 V；这不是供电实测值。
- 覆盖写入前完整读出 8,388,608 byte Flash 备份，保存在忽略目录 `firmware/out/device-com7/pre-evt-flash-8mb.bin`。
- 备份 SHA256：`65CB41E20E59DCB4211076D7F170DF2565CD731ABA2A5980DE0BB378E494A0C9`。
- 最新统一电脑测试17/17通过；C核心14套ASan及UBSan共28/28通过。
- 测试固件已在COM7烧录，bootloader/partition/app逐项写入哈希校验通过；应用USB命令正常。
- SD卡初始化与扇区读取成功：124,735,488个512-byte扇区，第一MBR分区type=0x07/start=32768，卷启动扇区EXFAT签名成立。用户确认是新购闪迪64GB。原FAT32固件挂载返回FatFs13（FR_NO_FILESYSTEM），不是没有卡、也不是没有格式化。
- 用户已选择增加exFAT支持，保留现有格式/数据。项目内兼容配置独立构建通过，14个FatFs消费者配置审计通过，C/C++ ABI探针通过；全局SDK跟踪文件保持干净。
- exFAT实卡挂载通过，卷总容量63,831,015,424byte，首次空闲63,830,622,208byte；此时未创建录音。初始MC100命名文件列表为空（不代表其他用户目录为空）。

## 麦克风独立采集实测

本表使用`capture`，只消费/统计PCM，不执行SD文件操作；`saved=0`。VAD禁用，左槽是初始配置，非零数据不能替代采样沿/听感验证。

| 时长 | 帧数/样本数 | 实际流程耗时 | 最小/最大样本 | 溢出/丢帧 | 队列峰值 |
| --- | --- | --- | --- | --- | --- |
| 3秒 | 150 / 48,000 | 3,043ms | 1267 / 3755 | 0 / 0 | 1 |
| 60秒 | 3,000 / 960,000 | 60,043ms | 1235 / 3772 | 0 / 0 | 1 |

两次结束内部空闲堆均173,988byte；Audio最小剩余栈5,052byte，Storage9,832byte；均正常静止并`reset_required=0`。耗时包括启动丢弃40ms，不是用已知音调测出的采样率精度。样本呈明显正偏置，后续须分析PCM均值/交流分量和实际声音，不能宣称声学质量通过。

采集固件app SHA256：`1343946879E3376BF9E1058F22BD982861B784896EF768858396CB22822A747A`。

## exFAT录音落盘与读回

本轮app SHA256：`00FC281AFEDA8C03148FDDDE650E8700AEAE7F0ED82DF50DBC44DDA4FD6BAF01`，317,456byte。SDK为锁定v6.1，ESP32-S3，无PSRAM，80MHz CPU，SDMMC1-bit20MHz。镜像与ELF位于`firmware/out/exfat-target/`。

首段3秒录音：150帧、48,000样本，零溢出/丢帧，队列峰值9，录制/收尾3,115ms；内部空闲堆130,940byte，Audio剩余栈5,048byte，Storage3,624byte。

音频96,512byte和索引2,304byte已通过COM7分块CRC读回。独立Python标准库wave及索引校验器确认：16000Hz/16bit/mono、3.000秒、所有BLOCK数据CRC/记录CRC/连续序号及FINAL通过，无INCIDENT。WAV SHA256 `5766010ebb8bd8f72d93e61056208c43bcb14aced6451374f1d1ddac9bc480a5`；IDX SHA256 `48417d1554a032d9ad1e031e23a4444fd65705d2a4e96f8c2b46883636554f03`。

原始PCM均值1311.19、AC RMS139.09，无削顶。启动瞬态后第2/3秒AC波动很小；没有已知音源/用户听音，声学效果仍不判PASS。数据校验成功证明传输/文件链路，不证明声音质量。

原始日志与环境音在忽略目录`firmware/out/device-com7/`，不上传远程。

## 仍需验证

- 多次录音及五分钟轮换，重启后已完成文件仍可读。

人工听感、人声灵敏度、电池校准、真实电源波形、整机功耗和耐久试验不由自动台架测试替代。未经运行的项目保持未验证。
